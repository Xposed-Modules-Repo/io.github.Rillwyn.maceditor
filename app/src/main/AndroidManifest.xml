<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      libxposed (Modern Xposed API / API 101) 模块：
      - 不再使用 legacy 的 xposedmodule / xposedminversion / xposedscope meta-data；
      - 模块名称使用 android:label，模块说明使用 android:description；
      - 作用域、API 版本等在 APK 内 META-INF/xposed/module.prop / scope.list / java_init.list 中声明。
    -->
    <application
        android:name=".App"
        android:label="@string/app_name"
        android:description="@string/xposed_description"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Material3.DynamicColors.DayNight.NoActionBar">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".MacBroadcastReceiver"
            android:exported="false" />
    </application>

</manifest>
