# سجل التغييرات (Changelog)

توثق هذه الصفحة جميع التعديلات والتحسينات المضافة على مشروع [MAC Editor](https://github.com/jqssun/android-mac-editor) الأصلي للمطور [jqssun](https://github.com/jqssun).

تخضع جميع التعديلات لشروط رخصة المشروع الأصلية (GPL-3.0)، مع الاحتفاظ بجميع حقوق النشر والملكية الفكرية الأصلية.

---

## [0.2.0] - 2026-09-03

### جديد (Added)
- **توافق شامل مع جميع الشركات المصنعة (من أندرويد 10 إلى أندرويد 16)**:
  - دعم كامل وعميق لجميع هواتف الشركات المصنعة: Google Pixel و Samsung (One UI) و Xiaomi / Redmi (MIUI / HyperOS) و Oppo / OnePlus / Realme (ColorOS / OxygenOS) و Vivo / iQOO (OriginOS / Funtouch OS) و Honor / Huawei (MagicOS / EMUI) و Motorola و Sony و Asus و Nothing و Transsion (Infinix / Tecno) و HTC و ZTE.
  - اكتشاف متعدد الطبقات لمحمل فئات الواي فاي (Wi-Fi ClassLoader) يشمل APEX `service-wifi.jar`، و `SystemServiceManager.loadClassFromLoader`، و `ServiceManager.addService("wifi")`، والانعكاس الديناميكي للخدمات، بالإضافة لدعم فئات HAL والطبقة الأصلية المخصصة لكل شركة (`WifiNative`، `WifiVendorHal`، `SemWifiNative`، `SemWifiVendorHal`، `MiuiWifiNative`، `MtkWifiNative`، `HwWifiNative`).
  - اكتشاف ديناميكي لواجهات نقطة الاتصال المحمولة (`ap*`، `softap*`، `swlan*`، `wlan1` إلخ) عبر فحص واجهات الشبكة وخطافات الاستدعاء الديناميكية لضمان تطبيق عنوان MAC لنقطة الاتصال بسلاسة عبر مختلف الشرائح والتعريفات.
- **تطبيق فوري وتلقائي بدون نقرات إضافية (Zero-Click Instant Apply)**:
  - بمجرد تبديل مفتاح "تجاوز عشوائية MAC" أو مفتاح "تجاوز MAC لنقطة الاتصال"، يتم تطبيق التغييرات فورياً على مستوى عتاد الواي فاي وواجهات الشبكة النشطة عبر بث IPC في أجزاء من الثانية، دون الحاجة للضغط يدوياً على زر "تطبيق عنوان MAC" أو إعادة تشغيل الواي فاي ونقطة الاتصال.
  - إشعارات تفاعلية فورية (Snackbars) لتأكيد التطبيق الفوري وتحديث حالة الكرت في الوقت الفعلي.
- **دعم اللغة العربية والواجهات من اليمين لليسار (RTL)**:
  - تعريب كامل وشامل لجميع عناصر الواجهة والنصوص والتنبيهات (`values-ar/strings.xml`).
  - دعم أصلي لاتجاه الواجهة من اليمين إلى اليسار (RTL) متوافق مع اتجاه النظام.
  - حماية حقول إدخال وعرض عناوين MAC وإجبارها على الاتجاه من اليسار لليمين (LTR) لمنع تشوه النصوص الست عشرية (Hexadecimal BiDi).
  - إضافة خيار التبديل الثلاثي السريع للغات في صفحة الإعدادات: English / 中文 / العربية.
- **إدارة المشروع والمطورين**:
  - تم التطوير والصيانة المشتركة بواسطة: **Rillwyn** و **م. عمرو الديب (Eng. Amr Eldeeb)**.

### تغييرات (Changed)
- ترقية الإصدار المستهدف إلى أندرويد 16 (`targetSdk = 37` و `compileSdk = 37`) مع الحفاظ على التوافق التام وصولاً إلى أندرويد 10 (`minSdk = 29`).
- استخدام `androidx.core.content.ContextCompat.registerReceiver` مع تعيين صريح لعلامة `RECEIVER_EXPORTED` للتوافق التام مع متطلبات أمان أندرويد 14+ و 16 وتجاوز فحوصات Lint الصارمة.
- تحديث نطاق Xposed (`scope.list` و `arrays.xml`) ليشمل تطبيق الإعدادات `com.android.settings` بجانب إطار عمل النظام `system`.
- ترقية رقم الإصدار إلى `0.2.0` (`versionCode` 11 → 12).

---

## [0.1.0] - 2026-08-26

### جديد (Added)
- **واجهة مستخدم جديدة بثلاث صفحات مع شريط تنقل سفلي ودعم السحب**:
  - إعادة هيكلة التخطيط الممتد إلى ثلاث صفحات منفصلة: **الرئيسية / الإعدادات / حول**، مع إمكانية التبديل عبر النقر على التبويب السفلي أو بالسحب يميناً ويساراً (باستخدام `ViewPager2`).
  - **الرئيسية**: كرت حالة الموديل، مفتاح تجاوز MAC العشوائي، كرت عناوين MAC (عنوان النظام / العنوان النشط / العنوان الاحتياطي / توليد عشوائي / زر التطبيق).
  - **الإعدادات**: اختيار اللغة، مفتاح فرض تفعيل عشوائية MAC، مفتاح تجاوز MAC لنقطة الاتصال.
  - **حول**: أيقونة التطبيق، اسم التطبيق، رقم الإصدار، روابط المشروع الحالي والمشروع الأصلي، ملاحظة المصدر والمطور.
- شريط الأدوات العلوي يتابع الصفحة الحالية تلقائياً.
- أيقونات متجهة جديدة لتبويبات التنقل السفلي.

### تغييرات (Changed)
- تحويل `MainActivity` إلى حاوية لإدارة شريط التنقل السفلي مع صفحات Fragments منفصلة (`HomeFragment`، `SettingsFragment`، `AboutFragment`).
- ترقية رقم الإصدار إلى `0.1.0` (`versionCode` 10 → 11).

### إصلاحات (Fixed)
- **استعادة ملفات `META-INF/xposed/` في حزمة Release APK**: استعادة `module.prop` و `scope.list` داخل `src/main/resources/META-INF/xposed/` لضمان تعرّف LSPosed على الموديل ونطاقه بشكل صحيح.

---

## [0.0.10] - 2026-08-25

### إعادة بناء أساسية
- **الاعتماد الكامل على YukiHookAPI 1.3.2**: إعادة بناء مدخل الموديل ومنطق الخطافات استناداً إلى آليات YukiHookAPI الرسمية، وحل مشاكل توافقية كانت تمنع بناء المشروع.
- استخدام `@InjectYukiHookWithXposed` وتوليد `assets/xposed_init` وفئات كشف الحالة آلياً عبر KSP.

### إصلاحات (Fixed)
- **إصلاح مشكلة ظهور "الموديل غير مفعّل" بعد إعادة التشغيل**: الاعتماد على `YukiHookAPI.Status.isModuleActive` لكشف الحالة الحقيقية.
- **إصلاح مشكلة عدم استجابة زر "تطبيق عنوان MAC" بعد الإقلاع**: خطف جميع مشيدات `WifiNative` وتخزين الكائن بمجرد إنشائه.
- **مشاركة الإعدادات عبر العمليات**: استخدام `YukiHookPrefsBridge` للقراءة والكتابة الآمنة بين تطبيق الموديل و `system_server`.
- **قراءة عنوان MAC الأصلي للمصنع ("System MAC")**: قراءة عنوان العتاد الحقيقي عبر الانعكاس وتقديمه فور فتح التطبيق.

---

## [0.0.8] - 2026-08-24

- دعم اللغات وتوفير مفتاح مخصص لتجاوز MAC لنقطة الاتصال المحمولة (AP).
- حل مشكلات عدم بدء نقطة الاتصال على بعض الأجهزة.
