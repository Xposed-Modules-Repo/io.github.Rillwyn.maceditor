# سجل التغييرات

يوثّق هذا الملف جميع التعديلات التي أُجريت على مشروع [MAC Editor](https://github.com/jqssun/android-mac-editor) الأصلي للمطوّر [jqssun](https://github.com/jqssun).

جميع التعديلات تمت بموجب شروط الترخيص الأصلي (AGPL-3.0)، مع الإبقاء على كافة إشعارات حقوق النشر الأصلية.

---

## [0.2.5] - 2026-09-05

### إضافات وتغييرات
- **دعم libxposed Modern Xposed API 102**: ترقية الاعتماديات إلى `102.0.0`؛ وضبط `targetApiVersion=102` و `minApiVersion=101` في `module.prop` لدعم أحدث معايير إطار Xposed الحديثة مع الحفاظ على التوافق التام مع أطر عمل API 101.
- **تبديل فوري وديناميكي لاتجاه RTL / LTR**: عند تغيير لغة التطبيق في الإعدادات (English / 中文 / العربية)، يتم تحديث اتجاه تخطيط الواجهة بالكامل (RTL ↔ LTR) فورًا دون الحاجة لإغلاق التطبيق يدويًا وإعادة تشغيله.
- **إصلاح التمرير وتجاوز الشاشة في صفحة حول**: تم حل مشكلة عدم إمكانية التمرير عند فتح القائمة القابلة للطي لـ **المساهمون** وتفاصيل كل إصدار. تمت إضافة أشرطة تمرير رأسية ومسافة أمان لمنع حجب المحتوى خلف شريط التنقل السفلي (`clipToPadding="false"` + `paddingBottom="96dp"`)، وتمرير تلقائي سلس عند فتح العناصر.
- تم رفع رقم الإصدار إلى `0.2.5` (`versionCode` من 14 إلى 17)؛ ومزامنة `module.prop`.

---

## [0.2.2] - 2026-09-05

### تغييرات (الواجهة)
- **الإعدادات — قائمة منسدلة للغات**: تم استبدال أزرار اختيار اللغة الثلاثة بقائمة منسدلة حديثة بتصميم Material 3 (`TextInputLayout` + `MaterialAutoCompleteTextView`): English / 中文 / العربية.
- **حول — مسؤول المستودع والمساهمون**: تعرض بطاقة المسؤول الآن **Rillwyn** كمسؤول للمستودع؛ وأُضيفت بطاقة **المساهمون** القابلة للطي والتي تدرج **Rillwyn** و **Eng. Amr Eldeeb**، مع تفصيل لإسهامات كل منهما في كل إصدار (باللغات الإنجليزية والصينية والعربية).
- تم رفع رقم الإصدار إلى `0.2.2` (`versionCode` من 13 إلى 14)؛ ومزامنة `module.prop`.

---

## [0.2.1] - 2026-09-05

### دمج مساهمات المجتمع (عبر [PR #1](https://github.com/Xposed-Modules-Repo/io.github.Rillwyn.android-mac-editor/pull/1) بواسطة [engamreldeeb](https://github.com/engamreldeeb))
- **توافقية متعددة الشركات**: يفحص الخطاف الآن حزم AOSP والشركات المعدّلة لـ `WifiNative` / `WifiVendorHal` (مثل Samsung `Sem*`، Xiaomi `Miui*`، MediaTek `Mtk*`، Huawei `Hw*`، وغيرها)، ويراقب `ServiceManager.addService("wifi")` لالتقاط محملات الفئات المتأخرة، ويكتشف واجهات نقطة الاتصال الديناميكية (`ap*`، `softap*`، `swlan*`، `wlanN`)، ويقرأ عنوان MAC المصنعي للشركات (Samsung EFS، Qualcomm `wlan_mac.bin`، وغيرها) — وكل ذلك نُقل إلى محرك libxposed API 101.
- **تطبيق فوري بدون نقرة**: ترسل تغييرات المفاتيح وعنوان MAC في الواجهة بث `ACTION_CONFIG_CHANGED`، ليقوم `system_server` فورًا بتطبيق عنوان MAC المخصص على واجهات STA و(اختياريًا) AP — دون الحاجة للنقر اليدوي على "تطبيق" أو إعادة تشغيل نقطة الاتصال.
- **دعم اللغة العربية وRTL**: ترجمة عربية كاملة `values-ar`، ودعم تخطيط الاتجاه من اليمين لليسار (`android:supportsRtl`)، وفرض اتجاه LTR لحقول MAC السداسية عشرية، وإضافة مبدّل لغات ثلاثي (English / 中文 / العربية) في الإعدادات.
- **تقدير الصيانة المشتركة**: تم تحديث المؤلف وصفحة حول إلى **Rillwyn & Eng. Amr Eldeeb**.

### تغييرات
- تم رفع رقم الإصدار إلى `0.2.1` (`versionCode` من 12 إلى 13)؛ ومزامنة `module.prop`.
- تحديث الوثائق: README (EN/CN/AR)، وسجل التغييرات CHANGELOG (EN/CN/AR)، وملاحظات الإصدار.

---

## [0.2.0] - 2026-09-05

### إعادة بناء جوهرية: الانتقال إلى libxposed Modern Xposed API (API 101)
- **مدخل وحدة حديث**: إزالة مدخل YukiHookAPI القديم (`HookEntry` / `@InjectYukiHookWithXposed` / معالج KSP / `assets/xposed_init`)؛ وإضافة `MacEditorModule : XposedModule` والمعلن عنه في `META-INF/xposed/java_init.list`.
- **استبدال تبعيات البناء**: استخدام `io.github.libxposed:api:101.0.1` (compileOnly) و `io.github.libxposed:service:101.0.0` (implementation) بدلاً من `com.highcapable.yukihookapi:*` وملف `libs/api-82.jar` المحلي؛ وإزالة معالج KSP. يعلن `module.prop` عن `minApiVersion=101` / `targetApiVersion=101` / `staticScope=true`؛ ونطاق `scope.list` يستهدف `system` (system_server).
- **تنظيف الـ Manifest**: إزالة بيانات التعريف القديمة `xposedmodule` / `xposedminversion` / `xposedscope` وفئة `MODULE_SETTINGS`. وأصبح اسم الوحدة ووصفها يؤخذان من `android:label` / `android:description`.

### تغييرات
- **التفضيلات عبر العمليات تعتمد الآن على التفضيلات البعيدة (Remote Preferences - قاعدة بيانات الإطار)**: يقرأ تطبيق الوحدة ويكتب عبر `XposedService.getRemotePreferences`، بينما تقرأ جهة الخطاف في `system_server` (للقراءة فقط) عبر `XposedModule.getRemotePreferences` وتسجل مستمعًا للتغييرات — مما يطبق الإعدادات فورًا دون إعادة تشغيل. ويُستخدم ملف `SharedPreferences` المحلي فقط كذاكرة تخزين مؤقت عند عدم التفعيل أو للمزامنة لمرة واحدة عند التفعيل.
- **كشف حالة التفعيل عبر XposedService**: تغيير `YukiHookAPI.Status.isModuleActive` إلى `App.isModuleActive()` (يدفع الإطار خدمته لتطبيق الوحدة فقط عندما تكون الوحدة مفعلة)؛ وتقوم الواجهة بتحديث بطاقة الحالة عبر مستمع حالة الخدمة.
- **سحب MAC النظام عبر البث**: استبدال `YukiHookDataChannel` بإرسال التطبيق بث `ACTION_QUERY_MAC` ويرد `system_server` بـ `ACTION_MAC_DETECTED`.
- **تحويل نموذج الخطاف إلى سلاسل اعتراض (Interceptor Chains)**: تغيير `Member.hook { before/after }` إلى `hook(Executable).intercept { chain -> … chain.proceed(newArgs) }`. يستمر تجاوز `Resources.getBoolean` عبر خطاف دالة عادي (الواجهة الحديثة أزالت استبدال الموارد — وهذه الوحدة لا تحتاجه).
- ضبط إعدادات البناء: `compileSdk` من 37 إلى 36 (تم التخلص من متطلب YukiHookAPI الصارم للإصدار 37)؛ وحذف `libs/api-82.jar` و`arrays.xml` وKSP و`android.suppressUnsupportedCompileSdk`.
- رفع رقم الإصدار إلى `0.2.0` (`versionCode` من 11 إلى 12؛ وتحديث `module.prop`).

### الوثائق
- تحديث توصيفات بنية YukiHookAPI وXposedBridge القديمة في `README.md` و`README_CN.md`.

### إضافات وتحسينات لاحقة
- **أيقونات بطاقة الحالة تحولت إلى متجهات Material Symbols الرسمية**: أيقونة `CheckCircle` للمفعّل، `Error` لغير المفعّل، `Warning` لتعطيل الخطاف.
- **كشف تفصيلي للحالة**: عند التفعيل، تعرض بطاقة الحالة اسم الإطار وإصداره، وإصدار Xposed API، والنطاق، وقدرات التفضيلات البعيدة (من `XposedService`)؛ وإضافة فرع تحذيري مخصص عند عدم شمول النطاق لـ `system` مع إرشادات تفاعلية في حالات عدم التفعيل أو إيقاف الخطاف؛ مع التراجع التلقائي للذاكرة المحلية في حال عدم توفر التفضيلات البعيدة دون حدوث استثناءات.
- **توقيع الإصدارات (Release Signing)**: إنشاء `keystore/maceditor-release.p12` محليًا لتوقيع الحزم آليًا بتوقيع v2.
- **توحيد النصوص الصغيرة وعناوين MAC في بطاقة الحالة**: استخدام `BodySmall` للعناوين الفرعية والصفوف الإضافية؛ ونقل عناوين MAC لسطر مخصص بخط أحادي المسافة (`status_mac` يظهر فقط عند التفعيل).
- **سلوك لوحة المفاتيح (IME)**: استخدام `windowSoftInputMode="adjustPan"` في `MainActivity` لضمان وضوح حقل الإدخال أثناء الكتابة وبقاء شريط التنقل السفلي في موقعه أسفل الشاشة.
- **تصميم Material لحقل إدخال MAC**: أصبح حقل "MAC الاحتياطي" بنمط Material 3 `TextInputLayout` (OutlinedBox) مع تلميح عائم وزر تفريغ النص **clear_text**. مع الحفاظ على جميع الوظائف السابقة (تصفية المحارف، التحويل التلقائي للأحرف الكبيرة، التنسيق الآلي، التوليد، والتطبيق).

---

## [0.1.0] - 2026-08-26

### إضافات
- **واجهة ثلاثية الصفحات مع شريط تنقل سفلي وتمرير سريع**: إعادة هيكلة التخطيط أحادي الصفحة إلى صفحات **الرئيسية / الإعدادات / حول** عبر `BottomNavigationView` و `ViewPager2` (التبديل بالنقر أو التمرير يمينًا ويسارًا). يتطلب تبعية `androidx.viewpager2` الجديدة.
  - **الرئيسية**: بطاقة حالة الوحدة، مفتاح "استبدال MAC العشوائي"، بطاقة عنوان MAC (MAC النظام / MAC النشط / حقل إدخال MAC الاحتياطي / توليد / تطبيق)، والملاحظة السفلية.
  - **الإعدادات**: اختيار اللغة مدمجًا (English / 中文)، ومفتاح "فرض تفعيل عشوائية MAC"، ومفتاح "استبدال MAC لنقطة الاتصال".
  - **حول**: أيقونة التطبيق، اسم التطبيق، قراءة الإصدار تلقائيًا من `BuildConfig.VERSION_NAME`، روابط هذا المشروع والمشروع الأصلي (اسم المشروع + الرابط الكامل، قابل للنقر للفتح في المتصفح)، ملاحظة المصدر، واسم المسؤول (Rillwyn).
- عنوان شريط الأدوات العلوي يتغير تلقائيًا وفق الصفحة الحالية.
- ثلاث أيقونات متجهة جديدة لألسنة التنقل السفلي (الرئيسية / الإعدادات / حول).

### تغييرات
- أصبحت `MainActivity` حاوية تدير محوّل `ViewPager2` والربط مع التنقل السفلي؛ ونُقلت قائمة اللغات من شريط الأدوات لصفحة الإعدادات مع تذكر الصفحة الحالية لتعود إليها بعد تبديل اللغة (`recreate()`).
- أجزاء جديدة `HomeFragment` و `SettingsFragment` و `AboutFragment` تحوي منطق الواجهة؛ ويعمل مستقبِل البث وسحب MAC النظام عبر DataChannel داخل `HomeFragment`.
- تم رفع رقم الإصدار إلى `0.1.0` (`versionCode` من 10 إلى 11).

### إصلاحات
- **استعادة ملفات `META-INF/xposed/` في حزمة Release APK** (`module.prop` و `scope.list`): استعادتها تحت `src/main/resources/META-INF/xposed/` لتضمينها في جذر الحزمة ليتعرف عليها LSPosed. يبقى `java_init.list` غير موجود قصدًا في هذا الإصدار القديم.

### الوثائق
- تحديث وثيقة `UI_REFACTOR_PLAN.md`.

---

## [0.0.10] - 2026-08-25

### إعادة بناء جوهرية
- **الترحيل الكامل إلى YukiHookAPI 1.3.2**: إعادة بناء مدخل الوحدة وتنفيذ الخطافات وفق آليات YukiHookAPI الرسمية، وإصلاح استدعاءات الواجهات البرمجية غير الموجودة في الشفرة القديمة.
  - المدخل الجديد `HookEntry` يستخدم `@InjectYukiHookWithXposed` + `IYukiHookXposedInit` مع توليد `assets/xposed_init` وفئات كشف الحالة آليًا عبر معالج KSP.

### إصلاحات
- **إصلاح ظهور "الوحدة غير مفعّلة" بعد إعادة التشغيل**: الاعتماد على `YukiHookAPI.Status.isModuleActive` لكشف الحالة الحقيقية من LSPosed مباشرة دون الاعتماد على إشارات البث المؤقتة.
- **إصلاح عدم استجابة زر "تطبيق عنوان MAC" بعد أول نقرة بعد التشغيل**: خطف جميع منشئات `WifiNative` وتخزين الكائن بمجرد إنشائه من النظام، مع دعم دوال `WifiVendorHal` البديلة.
- **إصلاح انقطاع التفضيلات عبر العمليات**: استخدام `YukiHookPrefsBridge` للمشاركة اللحظية لإعدادات `customMac` و `hookActive` و `apMacOverride` بين تطبيق الوحدة و `system_server`.
- **صلاحيات ملف التفضيلات**: تفعيل `isEnableHookSharedPreferences` لفرض وضع القراءة لملف التفضيلات (0664).

### تغييرات
- ضبط البناء: رفع `compileSdk` إلى 37 وإضافة KSP وحذف مستودع `maven.highcapable.me` المعطل.
- **خطف الموارد عبر `Resources.getBoolean`**: اعتراض `getBoolean(int)` في `system_server` وإرجاع true لأسماء الموارد المستهدفة.
- **إعادة كتابة الخطافات عبر `Member.hook`**: استخدام الانعكاس المباشر للعناصر المستهدفة لتجنب مشاكل دقة البحث في بعض البيئات.
- **تثبيت مزدوج للخطافات**: التثبيت عند `loadSystem` ومرة أخرى عند تحميل فئة `WifiService`.
- **عرض MAC المصنعي الحقيقي في "MAC النظام"**: استخدام الانعكاس لقراءة MAC المصنعي الفعلي للجهاز بدلاً من MAC العشوائي.
- **تطبيق MAC بنقرة واحدة فورية**: تمرير MAC المستهدف مباشرة عبر البث مع إعادة المحاولة التلقائية في حال تأخر جاهزية كائن `WifiNative`.
- **تحسين عرض الحالة**: إظهار العنوان المستخدم فعليًا في العنوان الفرعي لبطاقة الحالة.

### الوثائق
- تحديث `README.md` و `README_CN.md`.
- إضافة وثيقة ملخص الشفرة `CODE_SUMMARY.md`.
- رفع الإصدار إلى `0.0.10` في `app/build.gradle.kts`.

---

## [0.0.9] - 2026-08-24

### إصلاحات
- **إصلاح تفعيل إصدار Release**: تصحيح اسم الحزمة والنطاق في ملفات `META-INF/xposed` (`java_init.list` و `module.prop` و `scope.list`) لضمان حقن الوحدة في خادم النظام من قبل LSPosed.

---

## [0.0.8] - 2026-08-24

### إضافات
- **واجهة متعددة اللغات**: دعم اللغتين الإنجليزية والصينية مع إمكانية التبديل الفوري من القائمة.
- **مفتاح استبدال MAC لنقطة الاتصال (AP)**: مفتاح مستقل للتحكم في تطبيق MAC المخصص على واجهة نقطة الاتصال، مع جعله **معطلاً افتراضيًا** لتجنب فشل تشغيل نقطة الاتصال في الأجهزة التي ترفض تغيير MAC على `wlan2`.

### تحسينات
- **إصلاح توافقية نقطة الاتصال**: الاعتماد على اسم الواجهة (`wlan2`) بدلاً من فحص الدوال للتعرف بدقة على استدعاءات نقطة الاتصال.
- **تحسين تخزين تفضيلات اللغة**: حفظ لغة التطبيق في `SharedPreferences` المحلية لضمان استجابة التبديل الفوري.

### تفاصيل تقنية
- تعديل `WifiServiceHooker.kt` للتحقق من `ifaceName` وحالة مفتاح `apMacOverride`.
- تعديل `MainActivity.kt` لقراءة تفضيل اللغة في `attachBaseContext` واستخدام `recreate()` لتحديث الواجهة.
- إضافة ملف الموارد الصينية `values-zh/strings.xml`.
- تحديث `activity_main.xml` لإضافة بطاقة استبدال MAC لنقطة الاتصال.
- رفع الإصدار إلى `0.0.8` في `app/build.gradle.kts`.

### الوثائق
- تحديث `README.md` وإنشاء `README_CN.md`.
- إنشاء `CHANGELOG.md` و `CHANGELOG_CN.md`.

---

## الإصدارات الأصلية (المشروع الأصلي Upstream)

استند المشروع في بدايته إلى تطبيق `MacSposed` للمطوّر [David Berdik](https://f-droid.org/repo/com.berdik.macsposed_6_src.tar.gz)، ثم تم تحسينه وإتاحته كمشروع مفتوح المصدر بواسطة [jqssun](https://github.com/jqssun).

الميزات الأصلية شملت:
- خطف دوال `setStaMacAddress` و `setApMacAddress`.
- فرض موارد عشوائية MAC.
- واجهة مستخدم بسيطة لتعيين عنوان MAC مخصص.
